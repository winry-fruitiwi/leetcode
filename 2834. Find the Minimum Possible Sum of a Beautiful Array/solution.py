class Solution:
    # Given the beautiful array's length and target to avoid, return
    # the minimum sum of a beautiful array. A beautiful array has to
    # have a length of n and pairwise distinct positive integers and
    # no two distinct integers in the beautiful array can add up to
    # target.
    def minimumPossibleSum(self, n: int, target: int) -> int:
        # keep track of the current element

        # loop until n

            # add 1 to the current element

            # iterate through the previous elements. if any of them and
            # the current element add up to 1, increment the current element
            # and iterate again

            # keep track of the sum of the beautiful array
        
        # return the sum of the beautiful array
