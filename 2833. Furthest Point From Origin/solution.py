class Solution:
    def furthestDistanceFromOrigin(self, moves: str) -> int:
        # iterate through commands
            # use switch-case statement to check for each command
            
            # case R: increment
            
            # case L: decrement
            
            # case _: use negativity of current position to determine which direction to move
        
        # return absolute value of current position
