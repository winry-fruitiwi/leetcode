class Solution:
    def maximumDetonation(self, bombs: List[List[int]]) -> int:
        ### Creating the Bomb Graph ###

        # initialize graph, a graph of bombs

        # iterate through each bomb and check if it can detonate another bomb

            # iterate through each other bomb

                # if the current bomb will detonate another bomb:

                    # create a connection between the two bombs in the graph
